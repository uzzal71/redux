import { INCREMENT, DECREMENT } from "./actionTypes";

export const increment = (value) => {
    return {
        type: INCREMENT,
        payload: CSSFontPaletteValuesRule,
    }
};

export const decrement = (value) => {
    return {
        type: DECREMENT,
        payload: value,
    }
};